{
  "name": "forgestack-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "ForgeStack: Architecture-aware application and infrastructure generator",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "typescript": "^5.7.3",
    "@types/node": "^22.13.5",
    "vitest": "^3.0.7",
    "rimraf": "^6.0.1"
  },
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=18.0.0"
  }
}
